const QUIZ_PRICE_KZT = 990;

export function TribeQuizLanding({ locale }: { locale: string }) {
  const isKk = locale === 'kk';

  return (
    <div className="quiz-landing">
      <div className="quiz-landing-hero">
        <h1>
          {isKk ? 'Өз руыңызды ЖИ көмегімен табыңыз' : 'Узнайте свой род с помощью ИИ'}
        </h1>
        <p>
          {isKk
            ? 'Анкетаңыздағы барлық деректерді — тамға, ұран, аймақ, отбасылық әңгімелер — ЖИ еркін түрде талдап, бірнеше ықтимал ру нұсқасын ұсынады.'
            : 'ИИ анализирует все данные вашей анкеты — тамгу, уран, регион, семейные предания — в свободной форме и предлагает несколько вероятных версий вашего рода.'}
        </p>
        <a href="#quiz-start" className="btn btn-ai quiz-landing-cta">
          {isKk ? 'Өз руыңызды табыңыз' : 'Узнать свой род'}
        </a>
      </div>

      <div className="quiz-landing-steps">
        <div className="quiz-landing-step">
          <div className="quiz-landing-step-num">1</div>
          <h3>{isKk ? 'Анкета' : 'Анкета'}</h3>
          <p>
            {isKk
              ? '12 сұрақ: тамға, ұран, аймақ, білетін туыстарыңыз туралы еркін мәтін'
              : '12 вопросов: тамга, уран, регион, свободный текст о том, что вы знаете о родственниках'}
          </p>
        </div>
        <div className="quiz-landing-step">
          <div className="quiz-landing-step-num">2</div>
          <h3>{isKk ? 'ЖИ талдауы' : 'Анализ ИИ'}</h3>
          <p>
            {isKk
              ? 'ЖИ ережелер бойынша сәйкестікті және еркін мәтіндегі сигналдарды бірге қарастырады — жеке фактілер бойынша ғана емес'
              : 'ИИ рассуждает над совпадениями по правилам и качественными сигналами из свободного текста вместе — не по одному признаку'}
          </p>
        </div>
        <div className="quiz-landing-step">
          <div className="quiz-landing-step-num">3</div>
          <h3>{isKk ? 'Бірнеше нұсқа' : 'Несколько версий'}</h3>
          <p>
            {isKk
              ? 'Бір қатаң жауап емес — сенімділік деңгейі мен түсіндірмесі бар 1-5 ықтимал ру'
              : 'Не один жёсткий ответ, а 1–5 вероятных рода с уровнем уверенности и объяснением по каждому'}
          </p>
        </div>
      </div>

      <p className="quiz-landing-note">
        {isKk
          ? 'Бұл — отбасы үлкендерінен нақтылауға арналған болжам, соңғы шежірелік факт емес.'
          : 'Это гипотеза для уточнения у старших в семье, а не окончательный генеалогический факт.'}
      </p>

      <p className="quiz-landing-price">
        {isKk ? (
          <>Нәтиже — бір реттік төлем <span>{QUIZ_PRICE_KZT} ₸</span>, барлық сұраққа жауап бергеннен кейін</>
        ) : (
          <>Результат — разовая оплата <span>{QUIZ_PRICE_KZT} ₸</span>, после ответа на все вопросы</>
        )}
      </p>
    </div>
  );
}
