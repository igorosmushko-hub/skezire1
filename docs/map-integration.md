# Интеграция карты — 2026-09-05

Статус: общая навигация интегрирована и проверена локально на публичном дереве 47 родов. Выбор между этим деревом и полной базой из прежнего превью ещё не подтверждён пользователем. Полную базу не подключали; деплоя не было.

## Сделано

- Вход в `/[locale]/shezhire-tree` из меню и подвала на RU/KK. Существующие ссылки из энциклопедии и sitemap сохранены.
- Смена языка сохраняет query и hash, включая `highlight` и `join`.
- До 1280 px работает компактное меню: все пункты и вход доступны. При расширении окна меню закрывается и снимает блокировку прокрутки.
- Прямая ссылка на не загруженную ветвь в мобильной карте предлагает «Показать ветви», вместо ложного сообщения об отсутствии потомков.

## Фактически проверено

- `npm run build`: успешно, TypeScript и генерация 88 страниц.
- `node --test src/lib/tribe-tree.test.mjs src/lib/tribe-tree-page.test.mjs`: 8/8.
- `node scripts/seo-runtime-check.mjs`: успешно; ссылки RU/KK, query/hash, закрытие меню при переходе на desktop, незагруженная ветвь и настоящий лист. Проверка локальная, в CI не подключена.
- ESLint затронутых TSX и runtime-скрипта с временной flat-конфигурацией из установленных Next presets: успешно.
- `python3 scripts/seo-check.py http://127.0.0.1:3100`: 170 URL, метаданные/JSON-LD, ссылки, robots, редиректы — успешно. IndexNow не вызывался.
- Локальные API: 8 детей Наймана и поиск Адай в RU/KK; неверный источник и короткий поиск отклоняются, неизвестный узел возвращает 404.
- Браузер: 375/1024/1279/1280/1440 px; RU/KK без горизонтального переполнения, desktop-ссылки не обрезаны. Меню и вход доступны. Переход 1279 → 1280 снимает overlay и scroll-lock. RU → KK → RU сохраняет `highlight=naiman`; Найман раскрывает 8 ветвей на телефоне в обоих языках; поиск показывает Адай с путём.
- Независимое ревью изменений: замечаний нет. Адаптер полной базы в область этого ревью не входил.

## До выпуска

1. Уточнить версию карты. В исходном checkout есть незакоммиченный Neon-адаптер прежнего превью; здесь он не интегрирован. Для полной версии отдельно проверить приватность данных, раскрытие пути по внешнему ID и ошибки источника без незаметной подмены деревом 47 родов.
2. Восстановить общий lint/CI: свежий `npm run lint` падает, потому что использует удалённый `next lint`. Локальный ESLint выше не заменяет общий CI.
3. Устранить и проверить риск повторного начисления пакета: [payment-retry-risk.md](seo/payment-retry-risk.md). Платёжный код не изменён, реальная оплата не проводилась.
4. После определения итогового состава изменений: проверка preview с целевой конфигурацией и сценариями входа/вступления в род; сверка deployment и rollback. После выпуска — публичные HTTP/редиректы и GSC rendering по [протоколу измерений](seo/release-measurement.md).
