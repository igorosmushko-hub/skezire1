# Независимое ревью и завершение

2026-09-05. Read-only reviewer: специализированная роль `code-reviewer` (Sol/high), задача `research_review`. Reviewer проверил документы/сохранённый срез, не подключался к Neon и не выполнял runtime/build QA.

- Арифметика A18/B446/C294/D793836, E18 типов/7 традиционных связей/0 биологического родства и candidate path19 — согласована.
- Юридическая существенная правка: отражён действующий запрет нецелевого извлечения ст.7(11), исходный закон №231-VIII и изменение №326-VIII; исправлена ссылка на данные умершего ст.7(2). Reviewer успешно прочитал эти официальные нормы через браузер после ошибок основного web fetch. Квалификация конкретного сбора оставлена юристу.
- Замечание P2 по воспроизводимости внешнего evidence устранено основным агентом: добавлен `source-manifest.json`, source-level IDs, seed mapping, reviewer/matching basis, timestamp, SHA-256 двух успешно полученных источников. Оригиналы находятся только в `/private/tmp`, permissions 0600. Хеши и связь с seeds проверены `validate.py`. Постоянное закрытое хранение originals ещё не внедрено; после очистки временных файлов останутся hash/URL, но не гарантированная возможность повторно получить ту же версию.
- Низкий риск перезаписи результата при дублирующемся query ID устранён проверкой уникальности в runner и validator. Текущие 17 ID уникальны.
- После этих изменений основной агент заново выполнил `validate.py`, `node --check query-neon.mjs`, проверку локальных Markdown-ссылок и `git diff --check`. Ошибок нет. Проверки не подтверждают законность публикации, точность всех исходных данных или готовность production.

Чужой WIP основного checkout не менялся. В рамках этапа добавлен только каталог исследования и ссылка из `docs/genealogy-data-policy.md`; нет commit/push, deployment, DDL, изменения прав или is_public.
