# UX и SEO: проверочный пакет, 24–25.09.2026

## Основа

- Проверочный снимок кода: `origin/main` `3a3c9abe`; ветка `codex/ux-seo-pretest` не слита и не опубликована.
- В актуальном публичном образе от 24.09 07:09 UTC карта уже есть в шапке и подвале; показывает поиск и 340 ветвей. Повторный выпуск карты не нужен.
- Публичная карточка Starter всё ещё показывает 1 290 ₸ за 5 генераций, хотя владелец подтвердил 990 ₸. API тарифов и создание платежа читают цену из таблицы `packages`.
- Осмотр аудита покрывал шаблоны основных страниц RU/KK, 47 родов и 11 статей в обеих локалях. Генерация, покупка, авторизованные сценарии и поисковая консоль не проверялись.

## Что подготовлено к тесту

| Область | Изменение | Проверка до слияния |
|---|---|---|
| Меню и SEO-ссылки | Название публичной страницы — «Карта родов» / «Рулар картасы»; личное действие — «Создать шежіре». Существующие ссылки карты и тарифов сохранены. | RU/KK: десктоп и мобильное меню, шапка, подвал, переход на карту и назад. |
| Энциклопедия | На главной странице доступны все четыре группы, включая «Вне жузов»; вкладка сообщает активное состояние. Видимый справочный список даёт HTML-ссылки на все 47 карточек без необходимости запускать JS. | RU/KK: открыть каждую вкладку и карточки; убедиться, что доступны все 47 родов, а мобильная панель прокручивается. |
| Тарифы | Подготовлена миграция `008_starter_price_990.sql`: меняет Starter с 1 290 на 990 ₸ только при ожидаемом состоянии БД. API тарифов отдаёт цену без кеша, как и API оплаты; статичная разметка AggregateOffer удалена до отдельного выпуска цены, чтобы не закреплять неподтверждённую сумму. | Сначала проверить состояние `packages`; после отдельного применения миграции проверить `/api/packages`, карточку 990 ₸ / 198 ₸ за генерацию и сумму в checkout RU/KK. Не проводить реальную оплату без отдельного сценария теста. |
| Мобильные страницы | Верхние отступы у тарифов, заказа, юридических страниц и справочных/блоговых шапок подняты ниже закреплённой навигации. Блог, глоссарий и Жеті ата теперь явно загружают общие стили шапки: раньше их H1 оказывался под меню. | Прямое открытие RU/KK при 390×844 и 1440×900: H1, крошки, первый CTA, длинный заголовок статьи. |
| Создание шежіре | До ввода поясняется необходимость фотографии; файловый выбор и поля предков получили связанные подписи и управление с клавиатуры. | Клавиатура и экранный диктор; фото, род, семь предков, ошибки пустой формы. |
| Семейный портрет | Вместо шести обязательных на вид ролей сначала показаны два нейтральных участника; остальные добавляются по необходимости. | Загрузить 2, 3 и 6 фото в тестовом окружении; проверить удаление, повторную загрузку и отправку без списания реальных средств. |
| Заказ картины | Без изображения оставлен явный шаг создания AI-фото; форма выбора товара и оплаты показывается лишь при наличии изображения. Подписи адресных полей связаны с вводом. | Прямой URL без `image`, URL с результатом генерации, мобильный экран, итог заказа и ошибку API. |

## SEO после теста

- Проверить публичный HTML для карты, тарифов и всех четырёх групп энциклопедии: ссылки, canonical, hreflang, sitemap и robots. В коде `robots.ts` уже разрешает `/_next/`; старое замечание об этом не актуально для текущей ветки.
- Сопоставить индексацию, клики и запросы с GSC и Яндекс Вебмастером. Эти данные в аудите недоступны, поэтому эффект правок пока неизвестен.
- Измерить мобильные Core Web Vitals на опубликованной версии после выпуска. Локальные отступы и наличие ссылок не доказывают изменение трафика или скорости.

## Выполненные проверки

- Локальный SSR: HTTP 200 для `/ru/pricing`, `/ru/order/canvas`, `/ru/ai/family-portrait/create`, `/kk/encyclopedia`, `/ru/shezhire-tree`; через браузер проверены названия меню и наличие четырёх групп энциклопедии.
- В HTML `/ru/encyclopedia` и `/kk/encyclopedia` найдено по 47 уникальных ссылок на карточки родов; `/api/packages` отдаёт `Cache-Control: no-store`.
- TypeScript `tsc --noEmit --incremental false` — прошёл. ESLint затронутых TS/TSX — 0 ошибок; остались 3 прежних предупреждения о зависимостях `useCallback` в семейном портрете и предупреждение о счётчике Метрики в корневом layout. `git diff --check` — прошёл.
- В lockfile добавлена отсутствовавшая запись `@swc/helpers@0.5.23`; чистая установка `npm ci --ignore-scripts` прошла. Локальная production-сборка с Next.js 16.1.6 из lockfile прошла; остались предупреждения Next.js об Edge Runtime и устаревшем middleware.
- Для SEO задан `metadataBase=https://skezire.kz`; после этого сборка не предупреждает о подстановке `localhost:3000` в адреса Open Graph.
- В локальном браузере на узком экране проверены восемь шаблонов: H1 и крошки ниже навигации, горизонтального переполнения нет. Браузер ограничил фактическую ширину 480 CSS px, поэтому ручная проверка при 390 px остаётся обязательной.
- Проверены взаимодействия: вкладка «Вне жузов» показывает три карточки; мобильное меню ведёт на карту; без фото заказ не показывает оплату; семейный портрет начинает с двух участников и добавляет третьего; домашняя форма содержит семь связанных подписей предков и подпись фото.
- Миграция проверена на временной локальной PostgreSQL: 1 290 → 990 ₸, повторный запуск оставляет 990 ₸, неожиданные 1 990 ₸ вызывают ошибку и не меняются. Рабочая БД не затронута.
- Локальный сценарий цены проверен с временной имитацией Supabase и тестовым JWT: `/api/packages` вернул Starter 990 ₸ за 5 генераций и `no-store`; карточки RU/KK показали 990 ₸ и 198 ₸ за генерацию; `/api/payments/create` подготовил сумму `990.00` и тестовый параметр `IsTest=1`. Запрос не отправлялся в Robokassa, реальной оплаты и записи в рабочую БД не было.
- Сквозная генерация с двумя фото и реальный платёжный сценарий не запускались: в проверочной ветке нет подключения к тестовым AI/Robokassa сервисам, а рабочие платежи затрагивать нельзя до ручного теста.
- Одно синтетическое изображение успешно загрузилось в первый слот семейного портрета. Повторная попытка загрузить два синтетических файла тоже зависла внутри браузерного инструмента выбора файла, несмотря на таймаут. Полный сценарий с двумя файлами остаётся непроверенным; списаний и генерации не было.
- Локально `robots.txt` и `sitemap.xml` отдают HTTP 200. Sitemap структурно разобран: 170 URL, карта и тарифы есть в RU/KK. У карты, тарифов и энциклопедии в обеих локалях self-canonical и hreflang RU/KK; `/_next/` не запрещён в robots.
- Публичная карточка `/ru/pricing` повторно проверена после локального теста: Starter по-прежнему показывает 1 290 ₸ / 258 ₸ за генерацию, а пояснение ниже — 990 ₸. Это подтверждает, что проверочная ветка и миграция ещё не выпущены.

## Дополнение от 25.09.2026

### Исправления

- Страница и модальное окно тарифов используют общую загрузку без бессрочного клиентского кеша. При повторном открытии окна тарифы запрашиваются заново; отменённый запрос не перезаписывает результат следующего. Добавлены RU/KK-состояния загрузки, ошибки, пустого каталога и кнопка повтора. Заголовок модального окна связан с диалогом, закрытие имеет локализованную подпись.
- API тарифов и товаров возвращают `503` при недоступности БД/ошибке запроса и `200` при успешно полученном пустом каталоге. Все ответы — `no-store`. Во время локального теста обнаружено, что прежний `/api/products` отдавал закешированный пустой каталог даже после восстановления данных; причина устранена.
- В заказе печати видны загрузка/ошибка/пустой каталог, повторная попытка сохраняет введённый адрес. Оплата недоступна до загрузки выбранного товара и завершения проверки авторизации. Без изображения каталог не запрашивается.
- В sitemap добавлен публичный тест рода RU/KK. На этой странице вводный заголовок опроса заменён на H2: H1 остаётся у страницы, внешний вид сохранён.
- Исправлен контраст даты статьи и сообщений на светлой странице тарифов: используется `--text` (#1D1D1F).
- Языковые ссылки уже есть в HTML. Дублирование их во всём sitemap не добавлялось: Google считает HTML, HTTP и sitemap равнозначными способами и не требует применения всех трёх. Источник: https://developers.google.com/search/docs/specialty/international/localized-versions.

### Новая проверка

- `git ls-remote origin refs/heads/main`: основная ветка всё ещё `3a3c9abe3ac15b6750bbfb49ee2131b543716e3f`. Изменения остаются в `codex/ux-seo-pretest` без commit/push/merge/deploy.
- Итоговая production-сборка `npm run build -- --webpack` прошла, включая проверку TypeScript. ESLint файлов текущей доработки — без ошибок и предупреждений. `git diff --check` прошёл. Предупреждения Next.js о middleware и Edge Runtime остаются прежними.
- Полный локальный обход sitemap: **172 уникальных URL**, все HTTP 200, на каждом непустые title/description, один H1, self-canonical, языковые ссылки RU/KK, отсутствует noindex. Это техническая проверка HTML, а не подтверждение индексации или качества текста.
- Тестовые API packages/products: при сбое `503`, при пустом каталоге `200`, при доступном каталоге `200`; во всех шести случаях `Cache-Control: no-store`. Ответы не раскрывают внутреннюю ошибку БД.
- Браузер: тарифы RU прошли «ошибка → повтор → пустой список → повтор → Starter 990 ₸ / 198 ₸ за генерацию». Карточка и сообщение ошибки проверены также на KK.
- Браузер: заказ KK с локальным тестовым изображением прошёл «ошибка → пустой список → товар»; до загрузки товара кнопка оплаты отключена, после отображает доступность оплаты и итог 9 990 ₸ из тестового каталога. Оплата не нажималась.
- Проверен фактический цвет даты статьи и сообщения ошибки тарифов: `rgb(29, 29, 31)` на светлом фоне.
- Независимое статическое ревью нашло два замечания — цвет статусов и кеширование ошибок каталога печати. Оба исправлены и повторно проверены; других конкретных проблем в рассмотренных новых изменениях не найдено.

### Уточнение области работы владельцем

Владелец исключил генерации, вход, начисления и платёжные интеграции из текущей приёмки. Текущий пакет оценивается по публичным UX/UI и техническому SEO. Сквозные проверки интеграций остаются непроведёнными и не блокируют приёмку этого пакета. Миграция Starter 990 ₸ подготовлена ранее, но не применена; её выпуск и проверка оплаты — отдельный этап.

## Приёмка публичного UX/UI — 25.09.2026

### Доработано по результатам проверки

- У публичного теста рода заголовок находился на 51 px под меню высотой 64 px. Добавлен верхний отступ `calc(64px + 2rem)`; заголовок теперь ниже меню на мобильном и десктопе.
- Текущий раздел отмечен в основном и мобильном меню, включая вложенные страницы энциклопедии; добавлен `aria-current`.
- Кнопка меню имеет RU/KK-подпись открытия/закрытия и связь с мобильной панелью. Переключатель языка сообщает полное название языка и выбранное состояние.
- Добавлена видимая обводка клавиатурного фокуса. Вторичный текст `--muted` затемнён с #86868B до #62626B для читаемости на светлом фоне.
- Переходы к `#form-section` и `#quiz-start` учитывают закреплённую шапку через `scroll-margin-top: 80px`.

### Фактически проверено

- Браузер на этот раз подтвердил настоящую ширину **390 CSS px**; прежнее ограничение 480 px для этих проверок снято.
- **68 экранов**: 17 публичных маршрутов × RU/KK × 390/1440 px. Главная, тарифы, заказ без изображения, энциклопедия/жуз/род, блог/статья, глоссарий, Жеті ата, юридическая страница, тест рода, карта, рейтинг, AI-каталог и две страницы функций. Горизонтального переполнения, перекрытия H1 шапкой и обрезанного десктопного меню не найдено.
- Дополнительно при 1280 px меню не обрезается; на статье о роде активна «Энциклопедия».
- Мобильное меню открывается, переход закрывает панель. Escape закрывает меню, возвращает фокус кнопке и снимает недоступность основного содержимого.
- Смена RU → KK на `/encyclopedia/orta/argyn` сохраняет страницу и корректно меняет язык HTML/состояние переключателя.
- После нажатия CTA теста `#quiz-start` располагается на 79.95 px при нижней границе меню 64 px.
- Визуально осмотрены экран теста на 1440 px RU и 390 px KK: текст читаем, CTA и заголовок доступны.
- Обход ссылок из **172 страниц sitemap**: **175 уникальных внутренних адресов**, битых ссылок и отсутствующих якорей не найдено. Это проверка локальной сборки, не публичного сервера.
- Итоговая production-сборка прошла, ESLint изменённых компонентов навигации прошёл; `git diff --check` прошёл. Сохранены прежние предупреждения Next.js о middleware и Edge Runtime.
- До уточнения области работы проверено добавление шести пустых карточек участников портрета: без переполнения, генерация без фото отключена. Загрузка файлов и генерация в эту приёмку не входят.

### Статус

Публичный UX/UI и технический SEO-пакет готов к просмотру владельцем. Commit/push/merge/deploy не выполнены. Измерение поискового трафика, индексации и Core Web Vitals возможно отдельно после разрешённого выпуска; локальная проверка их улучшение не доказывает.

## Граница выпуска

Не пушить, не сливать и не публиковать ветку без разрешения владельца. Миграция цены исключена из PR и деплоя публичного UX/SEO-пакета. При отдельном выпуске цены сверить тариф и checkout на одной и той же новой версии приложения.

## Подготовка выпуска — 26.09.2026

Владелец разрешил создание GitHub PR и деплой UX/SEO на VPS. Слияние main не выполняется: выпуск из проверенной ветки с сохранением остальных серверных файлов. Миграция `008_starter_price_990.sql` остаётся локально вне коммита и пакета деплоя.

- В карте уточнено сообщение: «Продолжение этой ветви пока не добавлено» (и KK). Данные и глубина родов не менялись.
- Статичный `offers` удалён из JSON-LD тарифов; прежнее расхождение между текстом 990 ₸ и рабочей ценой 1 290 ₸ остаётся до отдельного выпуска цены.
- Финальные webpack build, 13 тестов дерева, genealogy-runtime-check, analytics-runtime-check и lint (0 ошибок, 13 существующих предупреждений) прошли.
- `encyclopedia-content-check` падает на `jalayir: biography outside accepted SEO09 scope`; `seo-runtime-check` — на `TribeCardEnc: empty symbol tile`. Обе ошибки воспроизведены на чистом HEAD основной ветки `3a3c9abe` с неизменёнными данными. Новые проверки меню и обновлённого сообщения листовой ветви в SEO-скрипте проходят до прежнего падения. PR должен оставаться draft до разбора этих baseline-проверок.
- Финальное независимое ревью: кодовых блокеров нет; проверяется включение двух новых UI-файлов и исключение миграции.
