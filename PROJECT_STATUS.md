# Шежіре — Статус проекта

> Последнее обновление: 26 февраля 2026
> Сессия: первый рабочий день

---

## Что такое Шежіре

Казахское генеалогическое древо онлайн — skezire.kz.
Пользователь выбирает жүз, ру, вводит имена 7 предков (жеті ата) и получает красивое генеалогическое древо.

**Стек:** vanilla HTML + CSS + JS, без фреймворков, без бандлера.
**Языки интерфейса:** казахский (основной), русский.
**Дизайн:** Playfair Display + Inter, цвета Blue #003082 / Gold #C8A84B / Off-white #F5F0E8.

---

## Структура файлов

```
skezire/
├── index.html          — главная страница (navbar, hero, about, form, tree, AI, footer)
├── style.css           — все стили (~680 строк)
├── script.js           — вся логика (~1340 строк)
├── data/
│   └── tribes.js       — TRIBES_DB: база казахских родов (uly/orta/kishi/other)
├── encyclopedia.html   — страница энциклопедии (ссылка в навбаре)
├── skezire.pen         — дизайн-файл для pencil.dev (полная главная страница)
└── PROJECT_STATUS.md   — этот файл
```

---

## Что было сделано сегодня

### 1. Shareable PNG для Instagram (основная задача)

**Проблема:** кнопка «Сохранить» была заглушкой — показывала тост «Ctrl+P».
**Решение:** полноценная генерация PNG 1080×1350 (Instagram portrait) через Canvas API.

#### Изменения в `script.js`:
- **Translation keys** — добавлены `tree.btn.share`, `share.generating`, `share.ready`, `share.error`, `share.title` для KK и RU
- **ZHUZ_COLORS** — цветовые схемы по жузам:
  - Ұлы жүз → синий (#001A4A)
  - Орта жүз → зелёный (#0A2E1A)
  - Кіші жүз → бордовый (#2E0A1A)
  - Other → синий (по умолчанию)
- **Canvas хелперы:**
  - `roundRect()` — с fallback для старых браузеров
  - `drawDiamond()`, `drawDiamondDivider()`, `drawDiamondGrid()` — орнамент из ромбов
  - `ensureFontsLoaded()` — гарантия загрузки шрифтов перед рендером
  - `drawRamHornMotif()`, `drawCornerOrnament()`, `drawOrnamentalBorder()` — кошкар мүйіз (бараньи рога) по периметру
  - `drawTamgaWatermark()` — полупрозрачная тамга рода 320px по центру
- **`drawTreeOnCanvas()`** — отрисовка 8 узлов (400×90px) с градиентами, glow, dashed borders, стрелками
- **`generateShareImage()`** — async, 9 слоёв композиции (фон → grid → рамка → заголовок → divider → тамга → дерево → инфо рода → branding)
- **`shareTree()`** — Web Share API с файлами → fallback на скачивание
- **`downloadTree()`** — заменена заглушка на реальную генерацию PNG

#### Изменения в `index.html`:
- Добавлен `<canvas id="share-canvas" width="1080" height="1350" style="display:none">`
- Кнопка «📤 Бөлісу» (Share) — новая primary CTA в `.tree-actions`
- «⬇ Сақтау» и «✎ Өзгерту» — переведены в outline
- В навбаре добавлена ссылка на `encyclopedia.html`

#### Изменения в `style.css`:
- `.btn-share` — золотой градиент, тень, hover
- `.btn-share.generating` — полупрозрачность + CSS-спиннер
- `@keyframes spin` — анимация спиннера

### 2. Дизайн-файл для Pencil.dev

Создан `skezire.pen` — полный дизайн главной страницы в формате pencil.dev:
- Desktop фрейм 1440px
- Все секции: navbar, hero, about, divider, form (с полями жеті ата), tree (с кнопками share/save/edit), AI cards, footer
- Дизайн-токены в variables (цвета, скругления, отступы)
- Точное соответствие CSS (градиенты, тени, шрифты, размеры)

---

## Что НЕ доделано / известные проблемы

1. **Git не инициализирован** — Xcode Command Line Tools не установлены. Нужно выполнить `xcode-select --install`, затем `git init` и первый коммит.
2. **Тестирование PNG** — нужно проверить в браузере:
   - Все 4 цветовые схемы (uly/orta/kishi/other)
   - Web Share API на мобильном
   - Скачивание на десктопе
   - Пустые предки (dashed nodes)
   - Оба языка (kk/ru)
   - Размер файла (<500KB)
3. **encyclopedia.html** — ссылка в навбаре добавлена, но сам файл не создавался в этой сессии.
4. **Мобильная навигация** — бургер-меню (`#nav-burger`) не имеет JS-обработчика (меню не открывается при клике).

---

## Планы на будущее

### Приоритет 1 — Технический долг
- [ ] Установить Xcode CLI Tools → git init → первый коммит
- [ ] Протестировать PNG-генерацию во всех сценариях
- [ ] Добавить обработчик бургер-меню для мобильных

### Приоритет 2 — Улучшения шеринга
- [ ] OG-изображение: автогенерация og:image для соцсетей
- [ ] QR-код на PNG картинке → ссылка на skezire.kz
- [ ] Варианты формата: Story (1080×1920), квадрат (1080×1080)

### Приоритет 3 — Новые фичи
- [ ] Энциклопедия родов (`encyclopedia.html`) — каталог всех жузов/родов с фильтрацией
- [ ] Сохранение данных: localStorage → возможность вернуться к заполненному дереву
- [ ] AI-функции (сейчас заглушки):
  - «100 лет назад» — стилизация фото
  - «Апашка в молодости» — де-эйджинг
  - «История рода» — генерация текста
- [ ] PDF-экспорт дерева
- [ ] Анимированное дерево (SVG → canvas transitions)

### Приоритет 4 — Рост
- [ ] SEO: sitemap.xml, robots.txt, мета-описания для подстраниц
- [ ] PWA: service worker, manifest.json, офлайн-доступ
- [ ] Аналитика: события на кнопки share/download/submit
- [ ] Домен и хостинг (skezire.kz)

---

## Контекст для AI-ассистента

- Пользователь общается на русском, контент проекта — казахский/русский
- Пользователь предпочитает автономную работу без лишних подтверждений (Yolo mode)
- Коммиты — только по явной просьбе
- Дизайн-система хранится в `skezire.pen` (pencil.dev)
- Все переводы — в объекте `TRANSLATIONS` в `script.js` (kk + ru)
- База родов — `TRIBES_DB` в `data/tribes.js`
